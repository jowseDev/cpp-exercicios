#include <iostream>

using namespace std;

int main()
{
    int a, b, temp;
    cout << "Entre os dois numeros: ";
    cin >> a >> b;
    cout << "Voce estrou com " << a << " e " << b << endl;

    temp = a;
    a= b;
    b=temp;
    cout << "Trocados, eles sao:" << a << e << b << end
    return 0;
}
